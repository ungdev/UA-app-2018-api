/*
const mongoose = require('mongoose');
const Schema = mongoose.Schema;

const spotlightSchema = new Schema({
  id: Schema.Types.ObjectId,
  name: String
})

var Spotlight = mongoose.model('Spotlight', spotlightSchema);
*/
